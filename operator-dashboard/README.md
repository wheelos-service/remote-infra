This folder contains the legacy `secure_control_sender.js` used by the previous operator dashboard.

Status: Deprecated — moved to `operator-ui/src/lib/secure-control-sender.ts` (TypeScript).

If you are migrating or removing legacy files, delete `secure_control_sender.js` after verifying `operator-ui` builds and the CI workflow is updated.
